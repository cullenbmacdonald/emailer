version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errname
    - errorlint
    - goconst
    - gocritic
    - misspell
    - noctx
    - prealloc
    - unconvert
    - unparam
    - whitespace

formatters:
  enable:
    - gofmt

issues:
  max-same-issues: 10
