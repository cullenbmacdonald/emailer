/// EmailClientKit is the shared Swift package for the Emailer clients.
///
/// It contains all API models, networking (REST + WebSocket), and local caching
/// used by both the macOS and iOS apps.
public enum EmailClientKit {
    /// The current version of the EmailClientKit package.
    public static let version = "0.1.0"
}
